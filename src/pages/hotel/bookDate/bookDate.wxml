<!--src/pages/hotel/bookDate/bookDate.wxml-->
<view>
    <datePicker MonthNumber="{{timeLimit}}" selectStart="{{searchData.startDate}}" selectEnd="{{searchData.endDate}}" bind:datePicker="datePicker"></datePicker>
</view>

